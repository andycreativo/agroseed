info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:5000/login
  body:
    type: json
    data: |-
      {
        "usuario": "andy",
        "contrasena": "claveIncorrecta"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
