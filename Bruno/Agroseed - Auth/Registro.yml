info:
  name: Registro
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:5000/registro
  body:
    type: json
    data: |-
      {
        "usuario": "andy",
        "contrasena": "claveSegura123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
