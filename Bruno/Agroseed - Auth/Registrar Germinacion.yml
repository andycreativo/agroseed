info:
  name: Registrar Germinacion
  type: http
  seq: 5

http:
  method: POST
  url: http://127.0.0.1:5000/api/lotes/LOT-API-001/germinacion
  body:
    type: json
    data: |-
      {
        "cantidad": 40
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
